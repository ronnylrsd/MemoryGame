import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    padding: 24,
  },
  title: {
    fontSize: 24,
    fontWeight: "bold",
    marginBottom: 20,
  },
  points: {
    fontSize: 20,
    marginTop: 20,
    marginBottom: 20,
  },
  gameOver: {
    fontSize: 30,
    marginBottom: 20,
  },
});
